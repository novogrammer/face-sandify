# 顔砂（かおすな）の当日説明
demo<br>
https://novogrammer.github.io/face-sandify/<br>
github<br>
https://github.com/novogrammer/face-sandify

## ステートメント
「顔砂」は、セルオートマトンによる砂粒の流動で顔の記録を生成し、消滅と再生を繰り返すインタラクティブ作品である。情報が物質化し、観客の存在が砂面に刻まれては消える過程を通じ、記憶・個人・社会の輪郭を曖昧に溶かしていく。砂上の模様の変化は、データ社会における記録と忘却の在りようを映し出す。

## 作品概要
Webカメラで取得した映像を砂粒のシミュレーションに変換し、崩壊と再構成を繰り返す様子を可視化するインタラクティブ作品。<br>
砂粒はセルオートマトンとして定義され、粒子の流動をthree.jsのTSL機能で実装したWebGPUベースのGPGPU処理によりリアルタイムで描画する。

## アルゴリズム部分
砂は順番に更新すれば矛盾は起きませんが、同時に更新すると粒子が同じセルを選んでしまうことがあります。<br>
特に左右をランダムに選ぶと、同じ場所を取り合う状況が生まれやすくなります。<br>
そこで、左右どちらを優先するかをフレーム単位で統一しました。
これにより取り合いが減り、処理を一度で完結できるようになりました。

## 技術部分
WebGLでも、テクスチャを交互に使う仕組み（ping-pong）を使えば、フラグメントシェーダーで砂の状態を更新できます。<br>
ただし本来は「絵を描くための流れ」の中で計算を行うことになるため、少し回り道になります。

今回は WebGPU を使い、「計算する工程」と「描画する工程」をはっきり分けました。<br>
まず Compute Shader で砂の状態を更新し、その結果を使って最後に描画します。<br>
役割を分離できるため、衝突処理や最適化を整理しやすくなり、構造もシンプルになります。<br>
<br>
また実装には three.js の TSL（Three.js Shader Language）を使用しました。<br>
シェーダーコードを直接書くのではなく、JavaScript / TypeScript から組み立てる形で記述することで、構造を保ちながら WebGPU 向けの処理を実装しています。